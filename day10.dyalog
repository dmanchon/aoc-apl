 day10←{
     input←↑⊃⎕NGET ⍵ 1
     f←{
         ⍺←⍬
         (≢⍵)=0:0

         a←1↑⍵
         b←1↑⍺

         ⍝ Errors
         (a=')')∧(b≠'('):3
         (a=']')∧(b≠'['):57
         (a='}')∧(b≠'{'):1197
         (a='>')∧(b≠'<'):25137

         ⍝ Correct close
         a∊'])}>}':(1↓⍺)∇(1↓⍵)
         ⍝ Open
         (a,⍺)∇(1↓⍵)
     }
     +/f¨↓input
 }
